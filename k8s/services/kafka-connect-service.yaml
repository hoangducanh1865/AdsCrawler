apiVersion: v1
kind: Service
metadata:
  name: kafka-connect
  namespace: marketing
spec:
  selector:
    app: kafka-connect
  ports:
    - name: api
      port: 8083
      targetPort: 8083
      nodePort: 30083
  type: NodePort
